let func1 = fn() -> i32, string {
    return 1, "hello";
};

let func2 = fn(c: u8) {
    return fn(a: i32, b: string) {
        print(a, b, c);
    };
};

func1() | func2(42)

// Output:
// 1 hello 42
